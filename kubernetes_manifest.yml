apiVersion: batch/v1
kind: CronJob
metadata:
  name: ${APPLICATION_NAME}
spec:
  schedule: "0 0 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: ${APPLICATION_NAME}
              image: ${CONTAINER_IMAGE_TAGGED}
              env:
                - name: FINANCES_API_URL
                  value: http://finances
                - name: FINANCES_API_ACCOUNT_ID
                  value: "1"
                - name: LOKI_URL
                  value: http://loki.monitoring:3100
              envFrom:
                - secretRef:
                    name: resona-scraper-env-from-vault
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: env
spec:
  refreshInterval: 5m0s
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: resona-scraper-env-from-vault
    creationPolicy: Orphan
  dataFrom:
    - extract:
        key: resona-scraper
